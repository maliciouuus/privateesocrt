^C
sed -i 's/ENV PYTHONDONTWRITEBYTECODE=1 \\/ENV PYTHONDONTWRITEBYTECODE=1 \\/' Dockerfile
^C
python -m pytest --cov=apps --cov-report=term-missing
^C
^C
^C
^C
^C
^C
^C
^C
^C
^C
^C
^C
^C
^C
^C
^C
^C
^C
^C
^C
^C
^C
cd /home/sacha/Public/openclassroom/privateesocrt && python3 -m pytest tests/unit/affiliate/test_models.py -v
^C
cd /home/sacha/Public/openclassroom/privateesocrt && flake8 tests/unit/affiliate/test_models.py
^C
cd /home/sacha/Public/openclassroom/privateesocrt && python -m flake8 tests/unit/affiliate/test_models.py
FROM python:3.10-slim

# Set environment variables
ENV PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1

WORKDIR /app

# Install system dependencies
RUN apt-get update \
    && apt-get install -y --no-install-recommends gcc python3-dev libpq-dev curl \
    && apt-get clean \
    && rm -rf /var/lib/apt/lists/*

# Install Python dependencies
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt \
    && pip install gunicorn

# Copy entrypoint script
COPY docker-entrypoint.sh /app/docker-entrypoint.sh
RUN chmod +x /app/docker-entrypoint.sh

# Copy project
COPY . .

# Create directories for static and media files
RUN mkdir -p /app/staticfiles /app/media

# Create logs directory
RUN mkdir -p /app/logs && touch /app/logs/affiliate.log && chmod -R 777 /app/logs

# Expose the port
EXPOSE 8000

# Use entrypoint script
ENTRYPOINT ["/app/docker-entrypoint.sh"]

# Command to run the application
CMD ["gunicorn", "--bind", "0.0.0.0:8000", "core.wsgi:application"] 
