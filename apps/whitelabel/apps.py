from django.apps import AppConfig


class WhitelabelConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.whitelabel"
    verbose_name = "Système de marque blanche"
